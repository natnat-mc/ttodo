package: ttodo
source:
    url: git://github.com/natnat-mc/ttodo
description:
    summary: Small todo list cli/gui
    detailed: >
        ttodo is a small tool to handle your todo list.
        It supports a CLI and a TUI.
        Requires tput and stty.
homepage: https://github.com/natnat-mc/ttodo
dependencies:
    - lua >= 5.1
build:
    type: builtin
    install:
        bin:
            ttodo: out/ttodo
